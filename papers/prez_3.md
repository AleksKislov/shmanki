## Слайд 1 — Титульный лист

Уважаемые члены комиссии. Тема моей выпускной квалификационной работы — Разработка обучающей платформы для разработчиков на основе интервального повторения с автоматической генерацией учебного контента.

## Слайд 2 — Актуальность темы и проблематика

Основной смысл работы - помочь программистам быстрее и эффективнее осваивать новые знания.

Проблемы, с которыми мы сталкиваемся при разучивании новых знаний:

- знания быстро забываются
- существующие системы и ПО для запоминания не учитывают структуру и специфику учебного материала
- из-за этого усилий и времени тратится больше, а результат хуже
- плюс знания в IT-сфере постоянно обновляются

## Слайд 3 — Цель

Поэтому главная цель моей работы — разработать ТАКУЮ обучающую платформу для разработчиков, которая будет ВО-ПЕРВЫХ использовать алгоритмы интервального повторения И ПРИ ЭТОМ учитывать структуру учебного материала.

## Слайд 4 — Задачи

Для этого решались следующие задачи:

1. Проанализировать теоретические основы интервального повторения и существующие решения в области запоминания знаний.

2. На основе анализа сформулировать требования к новой платформе, а также модель гетерогенного инфо-объекта.

3. Разработать модификацию алгоритма интервального повторения FSRS с учетом этой модели

4. Далее проектирование, реализация и тестирование

## Слайд 5 — Задача 1

С проблемами со слайда 2 столкнулся и я,
И ещё до ВКР я изучил многие доступные программы для запоминания.

Самыми полезными оказались программы на основе интервального повторения.

однако все они НЕ слишком хорошо подходят для запоминания сложной технической информации типа алгоритмов , структур данных и так далее.
Из этих программ я взял самое полезное, алгоритм для интервального повторения. Плюс я изучил исследования, касающиеся запоминания информации.

Для своей ВКР за основу я взял алгоритм FSRS, который является открытым и позволяет вносить изменения, что было важно для моей задачи.

## Слайд 6 — Задача 2

Рассмотренные мной программы запоминания все манипулируют карточками с вопросом и ответом, это хорошо работает для гомогенной простой информации, например, для запоминания иностранных слов.

Но для сложных технических концептов они слабо подходят.

Поэтому я ввожу понятие гетерогенного инфо-объекта.

Например, связный список - как раз ТАКОЙ инфо-объект, его класс, сигнатуры методов, вычислительная сложность, сами методы, все это лучше изучать по порядку.

Инфо-объект представлен в виде графа справа, вершинами которого будут отдельные знания о связном списке, то есть карточки с вопросами и ответами, а рёбрами - зависимости между этими знаниями. Чтобы начать изучение более сложных карточек, нужно сначала изучить более простые.

С такой моделью я уже сформулировал требования к системе.

Первые 3 требования связаны с управлением пользователями и их контентом.
4е требование - говорит о том, чтобы пользователи ДОЛЖНЫ сами выбирать правильный ответ.

5е требование - касается модификации алгоритма FSRS для работы с этой моделью.

и 6-е требование - автоматическая генерация учебного контента согласно модели.

## Слайд 7 — Задача 3

Из этой модели логически вытекают следующие модификации алгоритма FSRS:

во-первых: я ввел т.н. коэффициент иерархической поддержки для отдельной карточки. В базовом варианте он представляет собой взвешенное среднее степени усвоения всех предшествующих карточек.

Во-вторых, В новых алгоритмах, включая FSRS, сложность карточки также влияет на расчет интервалов повторения.

Поэтому я ввожу понятие эффективной сложности, которая вычисляется на основе базовой И с учетом коэффициента иерархической подддержки.

Третьей модификацией является то, как платформа считывает ответ. В моем приложении пользователю придется самому собирать ответ из токенов (то есть из кусочков, некоторые из которых могут быть неправильными).

В существующих же приложениях пользователь как правило выбирает между помню не помню, или выбирает степень насколько хорошо помнит: хорошо, плохо, не помню.

## Слайд 8 — Задача 4

Схему данных в БД как и сущности системы можно разбить на 6 основных:

это пользователь которому принадлежат колоды deck,

колоды же состоят НЕ просто из карточек, как в других системах, а из инфо-объектов,

которые в СВОЮ очередь представлены набором ВЗАИМОвосвязанных карточек.

Для карточек необходимо отдельно хранить их состояние, то есть насколько они хорошо усвоены, их прогресс.

Отдельным классом выделен сервис расчета интервалов повторения.

## Слайд 9 — Задача 5

Перед нами верхнеуровневая схема приложения,

состоит оно из фронтенда и бэкенда, который в свою очередь общается с БД и внешней LLM для генерации учебного контента.

Сам бэкенд разбит на модули по классам сущностей, которые я только что упоминал ПЛЮС генератор учебного контента.

Модули же разбиты на слои:
слой контроллеров,
сервисов,
И слой доступа к данным

## Слайд 10 — Задача 6

Ядро приложения - это сервис расчета интервалов, он досконально покрыт юнит тестами.

Плюс мне важно было протестировать пользовательские сценарии использования приложения.

На этом скриншоте видно результат генерации карточек для связного списка. Видно, что программа сгенерировала сразу много карточек, начиная с простых с прогрессией до сложных.

На следующем скриншоте видно, как выглядит сессия повторения таких карточек. Пользователю нужно самому выбирать ответ из набора токенов, некоторые из которых заведомо неверные.

## Слайд 11 — Выводы

- разработана платформа для разработчиков, упрощающая процесс запоминания знаний

- теоретическая новизна заключается в модификации алгоритма FSRS

- практическая польза и новизна заключается в сочетании генерации учебного контента, включая вопросы, над которыми нужно подумать, а не просто отвечать помню / не помню
