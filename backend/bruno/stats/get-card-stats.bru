meta {
  name: Get Card Stats
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/stats/card/{{cardId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Accept: application/json
}
