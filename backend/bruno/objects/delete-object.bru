meta {
  name: Delete Object
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/objects/{{objectId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Accept: application/json
}
