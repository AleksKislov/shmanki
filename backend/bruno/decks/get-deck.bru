meta {
  name: Get Deck
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/decks/{{deckId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Accept: application/json
}
